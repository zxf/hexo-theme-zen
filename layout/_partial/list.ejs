<ul>
  <% page.posts.each(function(item){ %>
    <li>
      <a data-pjax href="<%- url_for(item.path) %>"><%= item.title %></a>
      <span> [ <%= date(item.date, null) %> ] </span>
    </li>
  <% }); %>
</ul>